# Auto-detect text files and normalise line endings
  * text=auto

  # Force Windows line endings for Windows files
  *.bat text eol=crlf
  *.ps1 text eol=crlf

  # Force Unix line endings for cross-platform files
  *.sh text eol=lf
  *.yml text eol=lf
  *.yaml text eol=lf